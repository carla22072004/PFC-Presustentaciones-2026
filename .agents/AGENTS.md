# Reglas del Proyecto

## Git Commits
- Cada vez que se realicen mejoras o cambios, se debe hacer commit y push automáticamente al repositorio remoto.
- Los mensajes de los commits de Git deben ser genéricos (por ejemplo: "update" o "actualizacion") y no deben incluir nombres o detalles específicos del requerimiento solicitado por el usuario.
