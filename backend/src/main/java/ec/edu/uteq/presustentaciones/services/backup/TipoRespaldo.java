package ec.edu.uteq.presustentaciones.services.backup;

/** Tipo de respaldo. FASE 1 solo produce FULL; DIFERENCIAL queda reservado para la fase 2. */
public enum TipoRespaldo {
    FULL,
    DIFERENCIAL
}
